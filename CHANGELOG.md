# 更新日志 (Changelog)

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 风格。
版本号与 GitHub Release tag 对应（如 `v0.2.0`）。

---

## [v0.2.1] - 2026-09-01

### 新增：边缘端独立工作台（中心挂掉也可独立使用）

- **离线上传队列**：中心不可达时，上传自动进入本地队列目录（`--queue-dir`，默认 `./edge-queue`），文件与清单持久化到磁盘，不丢数据
- **自动补同步**：agent 每轮心跳循环与每次联网上传后自动冲刷队列到中心——文本走解析、二进制走对象存储，并可携带「上传后同步 Dify」标记一并补做
- **控制台离线降级**：状态卡显示「离线工作台模式」+「离线待同步 N 项」；上传结果明确提示「已本地排队（联网后自动补同步）」
- **离线文件名检索**：中心离线时检索降级为本地监听目录的文件名匹配（独立可用能力，非语义检索）
- **离线提示**：提案审阅 / Dify 同步 / 部类列表在离线时给出清晰提示（部类回退默认值）
- 新增 `GET /api/queue` 查看待同步队列

---

## [v0.2.0] - 2026-09-01

### 新增：新手动画教学（本次）

- **中心端控制台**：每次打开自动弹出 12 步动画教学，逐模块高亮讲解（管理员密钥 / 总览指标 / 模块导航 / 文件管理 / 知识检索 / 待添加清单 / 提案审核 / 告警 / 副本 / 系统与安全 / 后台配置 / 预留模块），支持上一步 / 下一步 / 跳过。
- **边缘端控制台**：每次打开自动弹出 6 步动画教学（通关信印 / 模块导航 / 文卷上传 / 经籍求索 / 卷宗批红 / 扩展象限），同样可跳过。
- 教学为纯前端自包含组件：无外部依赖、离线可用、不侵入业务逻辑。

### 本版本累计新增（含此前迭代）

**边缘端网页控制台（Go 内嵌，独立端口 9090）**
- 文件选择 / 分区上传（文本自动解析切片、二进制对象存储）
- 知识检索：结果带**参考链接**与**飞轮热度**（高频片段加权提升排序）
- **同步 Dify 数据集**：上传后自动同步或按卷册 ID 手动同步（未解析自动排队）
- **卷宗批红审阅（提案审批）**：列表 + 展读全文 + 准奏/驳回（经中心管理员信印鉴权）
- 古风纸墨风格前端（Tailwind 本地化，无 CDN、离线可用）

**中心端控制台**
- 模块化改版：总览 / 文件管理 / 知识检索 / 待添加清单 / 提案审核 / 告警 / 副本 / 系统与安全 / **后台配置总控台** / 预留模块
- 文件管理：分区筛选、搜索、切片查看、下载、**单个/全部同步到 Dify**
- 提案审核：点击标题展读完整正文后再批准/拒绝
- 预留模块入口（数据看板 / 自定义 / 智能体能力）

**知识库分区（分类）**
- 主机定义分区（`KNOWLEDGE_CATEGORIES`），文件带 `category` 字段（SQLite/PostgreSQL 幂等迁移）
- 上传可选分区、检索按分区过滤、文件管理按分区筛选

**Dify 驱动的问答链路（新飞轮形态）**
- 移除原 Python 数据飞轮（feedback/retrieval 事件、gaps 打分、proposal 生成）
- 严格接地：无参考片段命中时拒绝回答（`rag_strict`），防幻觉
- 参考链接：Dify 引用按「文档名=文件路径」映射回下载网关
- 待添加清单：定期汇总「常问但知识库未命中」的问题（worker 周期任务），可标记已添加/忽略
- 后台配置：Dify Base URL / API Key / 数据集 ID / 严格模式 / 最低相似度 / 热度加权 / 汇总周期（敏感键掩码回显）

**安全与工程**
- 边缘端控制台：Bearer 令牌鉴权（常数时间比较）、CSP/防点击劫持/nosniff、上传大小上限、路径防穿越、`[audit]` 审计日志
- 密钥不回显明文；`rag/ingest` 支持 admin 或 node 双鉴权（中心/边缘端均可上传数据集）
- 新增 `dify_client.py`（对齐官方 Dify Service API，含 `doc_form: text_model`）、`settings_store.py`（后台配置覆盖层）
- 前端重设计（中心端现代蓝、边缘端古风纸墨），全部内联/本地化资源

**构建与分发**
- Mac 开箱即用二进制（中心端 PyInstaller 26MB + 边缘端 Go 11MB，含网页控制台）
- GitHub Actions 发布流水线：中心端 Windows EXE / macOS(darwin-amd64/arm64)，边缘端 Windows/macOS 四平台（`make build-mac-center` 本地构建脚本）

### 修复

- 修复 Gemini 生成前端中的 JS 语法错误（多余反引号导致脚本整体失效）
- 修复 PyInstaller 单文件程序子进程残留导致端口占用的问题（文档说明 `pkill -f` 停止）
- 待添加清单仅收录「未命中」问题（不再混入命中项）

---

## [v0.1.0] - 2026-08-31

首个 MVP 基线（此前仓库已有）：

- 中心端：节点心跳/离线判定、文件报告→登记→解析→切片→副本流水线、任务系统（幂等/重试）、知识检索（Ollama 或确定性降级）、告警（4 渠道投递/抑制/确认）、RBAC/API 密钥/JWT/OIDC、备份恢复、移动控制面、运维控制台
- 边缘端：Go 二进制，心跳/文件清单上报/副本修复命令（Windows 服务模式）
- `make acceptance-smoke` 等验收设施
